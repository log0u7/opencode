stages:
  - security

default:
  interruptible: true

gitleaks:
  stage: security
  image:
    name: zricethezav/gitleaks:v8.30.1
    entrypoint: [""]
  script:
    - gitleaks detect --source "$CI_PROJECT_DIR" --verbose --redact
